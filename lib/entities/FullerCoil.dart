import 'package:coiler_app/database/drift_coil_database.dart';

class FullerCoil {
  TeslaCoil coil;
  //Primary? primary;

  FullerCoil(this.coil); //), this.primary);

  //FullerCoil.empty({this.coil, this.primary});
}
